name: Build and Test

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    uses: marcintk/ha-card-shared/.github/workflows/shared-build-and-test.yml@v2.0.1
    permissions:
      contents: read
      pull-requests: write
